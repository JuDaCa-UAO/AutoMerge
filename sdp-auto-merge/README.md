# SDP Auto Merge (POC) — Merge de 2 tickets con UI

## Requisitos
- Python 3.10+ recomendado

## Instalación
```bash
cd sdp-auto-merge
python -m venv .venv
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

pip install -r requirements.txt
